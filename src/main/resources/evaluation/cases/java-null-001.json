{
  "id": "java-null-001",
  "title": "Optional.get without presence check",
  "category": "RELIABILITY",
  "description": "User lookup uses Optional.get directly and can throw NoSuchElementException for missing users.",
  "repo": "demo-user-service",
  "prNumber": 102,
  "changedFiles": [
    {
      "filename": "src/main/java/com/demo/user/UserProfileService.java",
      "status": "modified",
      "additions": 7,
      "deletions": 1,
      "changes": 8,
      "patch": "@@ -12,6 +12,12 @@ public class UserProfileService {\n+    public UserProfile getProfile(Long userId) {\n+        User user = userRepository.findById(userId).get();\n+        return new UserProfile(user.getId(), user.getName(), user.getEmail());\n+    }\n }"
    }
  ],
  "contextFiles": [
    {
      "path": "src/main/java/com/demo/user/UserRepository.java",
      "content": "package com.demo.user;\n\nimport java.util.Optional;\n\npublic interface UserRepository {\n    Optional<User> findById(Long id);\n}\n"
    }
  ],
  "expectedFindings": [
    {
      "id": "expected-optional-get",
      "file": "src/main/java/com/demo/user/UserProfileService.java",
      "type": "RELIABILITY",
      "level": "MEDIUM",
      "title": "Optional.get may throw when the user does not exist",
      "keywords": ["Optional.get", "findById", "NoSuchElementException", "orElseThrow", "missing user"]
    }
  ]
}
