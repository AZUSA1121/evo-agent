{
  "id": "java-n-plus-one-001",
  "title": "N+1 query when loading order items",
  "category": "PERFORMANCE",
  "description": "The service queries order items inside a loop for each order.",
  "repo": "demo-order-service",
  "prNumber": 105,
  "changedFiles": [
    {
      "filename": "src/main/java/com/demo/order/OrderQueryService.java",
      "status": "modified",
      "additions": 10,
      "deletions": 1,
      "changes": 11,
      "patch": "@@ -25,6 +25,15 @@ public class OrderQueryService {\n+    public List<OrderView> listOrders(Long userId) {\n+        List<Order> orders = orderMapper.findByUserId(userId);\n+        return orders.stream()\n+                .map(order -> new OrderView(order, itemMapper.findByOrderId(order.getId())))\n+                .toList();\n+    }\n }"
    }
  ],
  "contextFiles": [
    {
      "path": "src/main/java/com/demo/order/OrderItemMapper.java",
      "content": "package com.demo.order;\n\npublic interface OrderItemMapper {\n    List<OrderItem> findByOrderId(Long orderId);\n    List<OrderItem> findByOrderIds(List<Long> orderIds);\n}\n"
    }
  ],
  "expectedFindings": [
    {
      "id": "expected-n-plus-one",
      "file": "src/main/java/com/demo/order/OrderQueryService.java",
      "type": "PERFORMANCE",
      "level": "MEDIUM",
      "title": "Order item query runs inside a loop",
      "keywords": ["N+1", "findByOrderId", "stream", "loop", "findByOrderIds"]
    }
  ]
}
